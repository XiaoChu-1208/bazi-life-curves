# 怎么用（用户视角）

这份文档面向**最终用户**——你只需在 Claude / Cursor 对话中说一句话，剩下的 skill 自动跑。
开发者请看 `SKILL.md`、安装请看 `INSTALL.md`。

---

## 30 秒速览

```
你：画一下八字 庚午 辛巳 壬子 丁未 男 1990 的人生曲线

Claude：
  ① 跑脚本算命局 + 三派打分 + 盲派事件 + 拐点              （5-10 秒）
  ② 给你 3 条"过往大事 / 本性"高把握命题让你先校验          ← ⚠ 必须等你回应
  你回："准 / 都对 / 继续 / 画图"
  ③ 在对话里**直接写出**整图综合分析 + 关键 10 年六维取象 + 争议年份解读
  ④ 把以上分析 + 6 条曲线 + 拐点表打包成交互式 HTML Artifact 给你
  你打开 Artifact，可以展开 / 收起每个 details 段
```

如果你在 Step ② 回答 "对不上 / 不准"，Claude 会**拒绝**继续画图，让你确认时辰是否准确。

---

## 触发短语（任选一种）

### A. 已经知道四柱
```
画一下八字 庚午 辛巳 壬子 丁未 男 1990 的人生曲线
画八字曲线 戊寅 甲辰 戊寅 庚申 男 1977
我的八字：丁巳 甲辰 戊寅 庚申，1977 年男，看看大运走势
```

### B. 只有公历生辰
```
我的公历生辰是 1990-05-12 14:30 男，看看 50 年大运曲线
1982-01-15 早上 9 点出生，女，画人生曲线图
```

### C. 含定制参数
```
画 30-55 岁这一段的曲线就行                 → --age-start 30 --age-end 55
看看未来 5 年的拐点                          → --forecast-window 5
分歧大的年份多标一些（敏感模式）              → --dispute-threshold 15
只看大趋势，别太敏感                         → --dispute-threshold 25
```

参数会被 Claude 自动识别，不需要你手写命令。

---

## 你会拿到什么

### 1. 一个交互式 HTML Artifact（在 Claude 窗口里直接打开）

```
┌──────────────────────────────────────────────────────────┐
│  八字人生曲线图 (xxxx xxxx xxxx xxxx)                      │
│  ─────────────────────────────────────                   │
│  6 条曲线（精神 / 财富 / 名声 × 当年 + 累积）              │
│  + 大运色带 + 置信带 + 三派争议黄柱 + ⚠ 盲派应事标记       │
├──────────────────────────────────────────────────────────┤
│  ▾ 整图综合分析（默认展开）                                │
│     命局基本盘 + 三条曲线形状 + 累积解读 + 命主定位         │
├──────────────────────────────────────────────────────────┤
│  ⚠ 派别争议年份                                           │
│     ▸ 1990 · ... · 名声维度（极差 22）                    │
│       三派打分条 + 分歧线索                                │
│       ▸ LLM 推论（点击展开 / 收起）  ← 默认收起           │
├──────────────────────────────────────────────────────────┤
│  未来 10 年关键拐点（按年份合并）                          │
│     2027 丁未 45 岁 [精神 ↑] [财富 →]  伤官见官            │
│     ▾ 2027 多维取象（默认展开）                            │
│        盲派应事（如有）                                    │
│        【职业】... 【关系】... 【财务】... 【健康】...      │
│        【心理】... 【时空】... 关键抉择 / 自处建议         │
└──────────────────────────────────────────────────────────┘
```

### 2. 同一份内容也会以 markdown 形式出现在对话里

方便复制 / 转发 / 搜索。

---

## 为什么需要 Step ② 校验？

八字最敏感的是**时辰**——出生时辰差 1 小时，月柱 / 时柱可能跳一位，
整个曲线就完全不同了。

所以 skill 会先抛 3 条"最高把握"的命题（比如"你 X 年应该有重大职业转折 / 你性格里某个特征"），
让你确认八字是否真的准。

| 你的回应 | Claude 行动 |
|---|---|
| **准 / 都对 / 继续 / 画图** | 直接进入绘图 |
| **对了 2 条 / 差不多** | 进入绘图，但加 caveat："结构对，年份强度需打折" |
| **只对 1 条 / 感觉不太对** | **拒绝绘图**，建议把出生时辰 ±1 小时各试一次 |
| **全错 / 都不对** | **拒绝绘图**，强烈建议复核八字本身 |

这是**硬门槛**——避免在错八字上做大量徒劳分析。

---

## 关于"LLM 推论"的几个常见疑问

### Q1：为什么有些年份的"LLM 推论"显示占位文字而不是真正的分析？

A：LLM 推论是 Claude **在对话里现写**的，理论上每个争议年份 + 每个拐点都应该写。
如果你看到占位符（"此处由 LLM 写入..."），说明：
- **打开的是示例 HTML**：示例只填了少数几个年份做演示（比如只填了 1990、2027、2032、2036）
- **Claude 在 Step 3a 偷懒了**：可以直接告诉它"把所有 disputes 和 turning_points 都写满"

### Q2：LLM 推论的内容靠谱吗？

A：靠 3 个机制保证：

1. **数字必须来自脚本**：LLM 不能自己给分数，只能解释脚本输出
2. **必须援引盲派应事 + 命局结构**：禁止"需要综合判断 / 因人而异"这种空话
3. **必须写可证伪点**：比如"如果该年并未发生 X，这一条就是错的"——便于你自己验证

详细规则见 `references/multi_dim_xiangshu_protocol.md` + `references/dispute_analysis_protocol.md`。

### Q3：为什么不能 LLM 直接打分？

A：因为这正是命理界的老问题——同一八字，不同师傅给出完全相反的判断。
本 skill 的设计哲学是：

- **"打分 = 算法 + 历史回测"**：保证公正、可重复、可证伪
- **"解读 = LLM + 强制协议"**：保证有论据、可质疑、能落到具体行动

把这两件事拆开，既不放弃命理的丰富性，也不让结果完全依赖某个 AI 的"灵感"。

### Q4：盲派为什么不进 25% 融合权重？

A：因为盲派擅长"应事 / 应期"（具体哪一年发生什么），
但**不擅长给整体能量打分**——盲派的师承本来也不做这件事。

所以 skill 把盲派定位为：
- **事件断**：识别 11 类经典组合（伤官见官 / 比劫夺财 / 财库被冲开 ...）
- **烈度修正**：在三派融合分上 ±8（重）/ ±4（中）/ ±2（轻）

详见 `references/mangpai_protocol.md`。

---

## 调参速查

| 我想... | 跟 Claude 说 |
|---|---|
| 看 30-55 岁这一段 | "画 30-55 岁的曲线" |
| 看未来 5 年（不要 10 年） | "看未来 5 年怎么走" |
| 看完整一生（80 岁） | "画整个一生的曲线" |
| 看近 10 年（含已发生 + 未来） | "近 10 年怎么走" |
| 把分歧年份标多一点 | "敏感一点 / 把所有可能争议的都标" |
| 只看大趋势 | "别太敏感 / 只标重要分歧" |
| 不要盲派加成 | "纯三派模式 / 关掉盲派" |
| 想看某一年的详细解读 | "给我详细讲一下 2030 年" |

---

## 不会用了？

直接问 Claude：
> 这个八字曲线 skill 怎么用？

它会按这份 USAGE.md 的内容回答你。

如果发现 bug 或想调整，开发者文档在 `SKILL.md`，规则文档在 `references/`。
